rootProject.name = 'horizpipes'
include 'utils'
include 'utils:converter'
findProject(':utils:converter')?.name = 'converter'
include 'utils:converter:debezium-test'
findProject(':utils:converter:debezium-test')?.name = 'debezium-test'

